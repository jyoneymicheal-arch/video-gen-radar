{
  "name": "video-gen-radar",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Daily-updated radar of active open-source video generation projects on GitHub.",
  "scripts": {
    "fetch": "node scripts/fetch.mjs",
    "brief": "node scripts/brief.mjs",
    "build": "node scripts/build.mjs",
    "pipeline": "node scripts/fetch.mjs && node scripts/brief.mjs && node scripts/build.mjs",
    "serve": "node scripts/serve.mjs",
    "verify": "node scripts/verify.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
